import { LevelDown as LevelDOWN } from "leveldown";
import { IProc } from "./proc";
export declare function reclaimProc(db: LevelDOWN, activeProcs: IProc[], procName: string, callback: (err?: Error | null, lastClaimedRange?: string) => void): void;
